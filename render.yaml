services:
  - type: web
    name: reachify-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build && cd ../frontend && npm install --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
